<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calculadora de IMC</title>
</head>

<body>
    <div class="container">
        <h2>Calcule seu IMC</h2>
        <input type="number" id="peso" placeholder="Peso (kg)">
        <input type="number" id="altura" placeholder="Altura"> <br> <br>

        <button onclick="calcularIMC()">Calcular IMC</button>

        <div class="resultado" id="resultado"></div>
    </div>

    <style>
        body {
            font-family: Helvetica, Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background-color: #f7f7f7;
        }

        .container {
            background-color: #fff;
            padding: 30px;
            border: 2px solid #ccc;
            width: 100%;
            max-width: 350px;
            text-align: center;
        }

        h1 {
            color: black;
            margin-bottom: 20px;
            font-weight: 300px;
            font-size: 1.8em;
        }

        .input-type {
            margin-bottom: 15px;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: grey;
            font-size: 0.9em;
        }

        input[type="number"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #aaa;
            box-sizing: border-box;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: black;
            color: white;
            border: 1px solid;
            cursor: pointer;

        }

        button:hover {
            background-color: black;
        }

        .resultado {
            margin-top: 25px;
            padding: 15px;
            border: 1px grey;
            background-color: #fff;
            font-size: 1em;
            min-height: 30px;
        }
    </style>

    <script>
        function calcularIMC() {
            const peso = parseFloat(document.getElementById('peso').value);
            const altura = parseFloat(document.getElementById('altura').value);

            if (!peso || !altura) {
                alert("Por favor, preencha todos os campos.");
                return;
            }

            const imc = peso / (altura * altura);
            const imcFixado = imc.toFixed(2);
            let classificacao = "";
            let mensagem = "";

            if (imc < 18.5) {
                classificacao = "Palito";
                mensagem = ("Vai comer algo, plmdd")
            } else if (imc >= 18.5 && imc <= 24.9) {
                classificacao = "Normal";
                mensagem = ("Finalmente um normal")
            } else if (imc >= 25 && imc <= 29.9) {
                classificacao = "Gordola";
                mensagem = ("Trooopaa do Gordãooo")
            } else {
                imc >= 30
                classificacao = "Titanic";
                mensagem = ("Academia q é bom nada né")
            }

        document.getElementById('resultado').innerHTML = `
            Seu IMC é: ${imcFixado}<br>
            Classificação: ${classificacao}<br>
            Mensagem : ${mensagem}
        `;
        }
    </script>
</body>

</html>
